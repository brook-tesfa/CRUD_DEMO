# City Management API

A clean, RESTful Express.js API built using the **MVC (Model-View-Controller)** pattern. This project manages a simple collection of cities stored in memory.

---

## 🚀 Features
* **Full CRUD**: Create, Read, Update, and Delete city resources.
* **MVC Architecture**: Separated concerns for scalability.
* **Middleware**: Includes a request logger, data validation, and global error handling.
* **Developer Friendly**: Standardized JSON responses and HTTP status codes.

---

